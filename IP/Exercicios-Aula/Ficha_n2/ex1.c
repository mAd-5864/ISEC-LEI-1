#include <stdio.h>

int main(){

float raio;
float per;
float area;
const float pi=3.14159

scanf("Introduza um valor: %f", raio);

per = 2*pi*raio;
area = pi*r^2;

printf("Perimetro: %.2f\n",per);
printf("Area: %.2f\n",area);
}